- hosts: satellite-test
  vars:
        foreman_version: 1.15
        foreman_ssl: true
        foreman_servername: "{{ ansible_nodename }}"
        foreman_admin_password: AWGtemp!
        foreman_oauth_consumer_key: DGIref4Sh
        foreman_ouath_consumer_secret: DGIref4Sh
        foreman_db_password: DGIref4Sh 
  roles:
        - { role: foreman }
